//
//  UserViewModel.swift
//  Babylo
//
//  Created by houda lariani on 16/3/2023.
//

import Foundation
// Alamofire

class SignUpViewModel: ObservableObject {
    
   /* func signUp(username: String, password: String, email: String, completion: @escaping (Result<String, Error>) -> Void) {
        let url = "http://localhost:8080/user/signup"
        let parameters: [String: Any] = [        "username": username,        "password": password,        "email": email    ]
        AF.request(url, method: .post, parameters: parameters, encoding: JSONEncoding.default).responseJSON { response in
            switch response.result {
            case .success:
                completion(.success("Account activation link sent."))
            case .failure(let error):
                completion(.failure(error))
            }
        }
    }*/
}
